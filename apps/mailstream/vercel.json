{
    "version": 2,
    "builds": [
      {
        "src": "dist/index.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/dist/index.js"
      }
    ],
    "buildCommand": "npm install && cd ../../packages/db && npx prisma generate && cd ../../apps/mailstream && npx tsc -b",
    "outputDirectory": "dist"
  }