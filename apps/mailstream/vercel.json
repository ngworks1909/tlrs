{
    "version": 2,
    "builds": [
      {
        "src": "/**/*",
        "use": "@vercel/node",
        "config": {
            "buildCommand": " ls && npx tsc -b"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ]
  }