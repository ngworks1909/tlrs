{
  "version": 2,
  "builds": [
    {
      "src": "./src/**/*.ts",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "ls && npm install && npx tsc -b",
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/$1"
    }
  ]
}