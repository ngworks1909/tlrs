{
  "version": 2,
  
  "builds": [
    {
      "src": "./src/**/*.ts",
      "use": "@vercel/node",
      "buildCommand": "npm install && cd ../../ && npm install && cd packages/db && npx prisma generate && cd ../../apps/mailstream && npx tsc- b"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}